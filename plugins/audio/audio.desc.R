## this is the .desc.R file for the "audio" plugin.

list(
     ## the plugin version
     version = "0.01",
     
     ## a label that appears in the "Load a plugin" menu, describing the plugin
     label = "a simple audio sampling plugin for radR"
     
)

